@model IEnumerable<_24DH113423_MyStore.Models.Order>

@{
    // 1. Đặt tiêu đề trang
    ViewBag.Title = "Quản lý Đơn hàng";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<p class="mb-4">
    @Html.ActionLink(" Thêm đơn hàng mới", "Create", null, new { @class = "btn btn-primary" })
</p>

<div class="card shadow-sm">
    <div class="card-body">

        <div class="table-responsive">

            <table class="table table-striped table-hover align-middle">

                <thead class="table-dark">
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.Customer.CustomerName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.OrderDate)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.TotalAmount)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.PaymentStatus)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.AddressDelivery)
                        </th>
                        <th class="text-end" style="width: 200px;">Hành động</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Customer.CustomerName)
                            </td>

                            <td>
                                @item.OrderDate.ToString("dd/MM/yyyy")
                            </td>

                            <td>
                                @item.TotalAmount.ToString("N0")đ
                            </td>

                            <td>
                                @if (item.PaymentStatus == "Đã thanh toán")
                                {
                                    <span class="badge bg-success">@item.PaymentStatus</span>
                                }
                                else if (item.PaymentStatus == "Chờ xử lý")
                                {
                                    <span class="badge bg-warning text-dark">@item.PaymentStatus</span>
                                }
                                else if (item.PaymentStatus == "Đã hủy")
                                {
                                    <span class="badge bg-danger">@item.PaymentStatus</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">@item.PaymentStatus</span>
                                }
                            </td>

                            <td>
                                @Html.DisplayFor(modelItem => item.AddressDelivery)
                            </td>

                            <td class="text-end">
                                @Html.ActionLink("Sửa", "Edit", new { id = item.OrderID }, new { @class = "btn btn-sm btn-warning" })
                                @Html.ActionLink("Chi tiết", "Details", new { id = item.OrderID }, new { @class = "btn btn-sm btn-info ms-1" })
                                @Html.ActionLink("Xóa", "Delete", new { id = item.OrderID }, new { @class = "btn btn-sm btn-danger ms-1" })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

        </div>
    </div>
</div> ```

<!-----

###-->
@*🎨 Giải thích các thay đổi

    * **Đồng bộ Layout:** Dùng `card`, `card-body`, `table-responsive`, `table-striped`, `table-dark` để đồng bộ với các trang `Index` khác.
    * **Định dạng Ngày:** Dùng `@item.OrderDate.ToString("dd/MM/yyyy")` để chuyển định dạng ngày giờ sang `Ngày/Tháng/Năm`.
    * **Định dạng Tiền:** Dùng `@item.TotalAmount.ToString("N0")` để thêm dấu phẩy hàng nghìn.
    * **Định dạng Trạng thái:** Dùng logic `@if` để kiểm tra `item.PaymentStatus`. Tùy thuộc vào giá trị(ví dụ: "Đã thanh toán", "Đã hủy"...), nó sẽ hiển thị một "badge"(nhãn) với màu sắc tương ứng(`bg - success`, `bg - danger`...).
    * **Các nút: **Các link `ActionLink` được đổi thành `btn` (nút)với màu sắc và kích thước(`btn - sm`) phù hợp.*@
