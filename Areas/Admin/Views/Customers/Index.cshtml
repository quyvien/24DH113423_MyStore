@model IEnumerable<_24DH113423_MyStore.Models.Customer>

@{
    // 1. Đặt tiêu đề trang
    ViewBag.Title = "Quản lý Khách hàng";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<p class="mb-4">
    @Html.ActionLink(" Thêm khách hàng mới", "Create", null, new { @class = "btn btn-primary" })
</p>

<div class="card shadow-sm">
    <div class="card-body">

        <div class="table-responsive">

            <table class="table table-striped table-hover align-middle">

                <thead class="table-dark">
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.CustomerName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.CustomerPhone)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.CustomerEmail)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.CustomerAddress)
                        </th>
                        <th class="text-end" style="width: 200px;">Hành động</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.CustomerName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.CustomerPhone)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.CustomerEmail)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.CustomerAddress)
                            </td>

                            <td class="text-end">
                                @Html.ActionLink("Sửa", "Edit", new { id = item.CustomerID }, new { @class = "btn btn-sm btn-warning" })
                                @Html.ActionLink("Chi tiết", "Details", new { id = item.CustomerID }, new { @class = "btn btn-sm btn-info ms-1" })
                                @Html.ActionLink("Xóa", "Delete", new { id = item.CustomerID }, new { @class = "btn btn-sm btn-danger ms-1" })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

        </div>
    </div>
</div> `
